<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MyBeast.ViewModels.Workout"
             xmlns:model="clr-namespace:MyBeast.Domain.Entities;assembly=MyBeast.Domain"
             x:Class="MyBeast.Views.Workout.WorkoutListPage"
             Title="Histórico de Treinos"
             x:DataType="vm:WorkoutListViewModel">

    <Grid RowDefinitions="*, Auto" Padding="10">

        <RefreshView Grid.Row="0" 
                     Command="{Binding LoadHistoryCommand}"
                     IsRefreshing="{Binding IsBusy}">
            <CollectionView ItemsSource="{Binding History}"
                            SelectionMode="None">
                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                        <Label Text="Nenhum treino registrado ainda." 
                               TextColor="Gray" HorizontalOptions="Center"/>
                        <Label Text="Seja uma besta, comece agora!" 
                               TextColor="Gray" HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:WorkoutSession">
                        <Frame Margin="0,5" Padding="15" CornerRadius="10" HasShadow="True">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:WorkoutListViewModel}}, Path=GoToDetailsCommand}"
                                                      CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>

                            <Grid ColumnDefinitions="*, Auto">
                                <VerticalStackLayout Grid.Column="0" Spacing="2">
                                    <Label Text="{Binding Date, StringFormat='{0:dd/MM/yyyy}'}" 
                                           FontAttributes="Bold" FontSize="16" />
                                    <Label Text="{Binding TotalVolume, StringFormat='Volume Total: {0} kg'}" 
                                           FontSize="12" TextColor="Gray"/>
                                </VerticalStackLayout>

                                <Label Grid.Column="1" 
                                       Text="{Binding DurationMinutes, StringFormat='{0} min'}" 
                                       VerticalOptions="Center" 
                                       TextColor="{StaticResource Primary}" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

        <Button Grid.Row="1"
                Text="INICIAR NOVO TREINO"
                Command="{Binding StartNewWorkoutCommand}"
                Margin="0,10,0,0"
                HeightRequest="50"
                CornerRadius="25"
                FontAttributes="Bold"/>
    </Grid>
</ContentPage>