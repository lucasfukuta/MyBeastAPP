<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MyBeast.ViewModels.Diet"
             xmlns:models="clr-namespace:MyBeast.Models"
             x:Class="MyBeast.Views.Diet.MealEditorPage"
             Title="{Binding PageTitle}"
             BackgroundColor="#1C1C1E">

    <ContentPage.BindingContext>
        <vm:MealEditorViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, *, Auto">
        <VerticalStackLayout Grid.Row="0" Spacing="15" Padding="20">
            <Label Text="Nome da Refeição" TextColor="Gray"/>
            <Entry Text="{Binding Name}" TextColor="White" BackgroundColor="#2C2C2E" Placeholder="Ex: Jantar" FontAttributes="Bold" FontSize="18"/>

            <Label Text="Horário" TextColor="Gray"/>
            <TimePicker Time="{Binding Time}" TextColor="White" BackgroundColor="#2C2C2E"/>

            <BoxView HeightRequest="1" Color="#333" Margin="0,10"/>

            <Grid ColumnDefinitions="*, 80, 40">
                <Label Text="Alimento" TextColor="Gray" FontAttributes="Bold"/>
                <Label Grid.Column="1" Text="Kcal" TextColor="Gray" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
            </Grid>
        </VerticalStackLayout>

        <CollectionView Grid.Row="1" ItemsSource="{Binding FoodItems}" Margin="20,0">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:FoodItem">
                    <Grid ColumnDefinitions="*, 80, 40" Margin="0,0,0,10">

                        <Border Grid.Column="0" BackgroundColor="#2C2C2E" StrokeShape="RoundRectangle 8" Padding="10,0" Margin="0,0,5,0">
                            <Entry Text="{Binding Name}" Placeholder="Item" TextColor="White" BackgroundColor="Transparent"/>
                        </Border>

                        <Border Grid.Column="1" BackgroundColor="#2C2C2E" StrokeShape="RoundRectangle 8" Padding="5,0" Margin="0,0,5,0">
                            <Entry Text="{Binding Kcal}" Keyboard="Numeric" TextColor="#FFA500" BackgroundColor="Transparent" HorizontalTextAlignment="Center"/>
                        </Border>

                        <Button Grid.Column="2" Text="-" 
                                BackgroundColor="#FF453A" TextColor="White" 
                                Padding="0" CornerRadius="8"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MealEditorViewModel}}, Path=RemoveItemCommand}"
                                CommandParameter="{Binding .}"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <VerticalStackLayout Grid.Row="2" Padding="20" Spacing="15" BackgroundColor="#252525">

            <Button Text="+ Adicionar Alimento" 
                    Command="{Binding AddNewItemCommand}" 
                    BackgroundColor="#333" TextColor="White"/>

            <Border BackgroundColor="#5A2E8B" StrokeShape="RoundRectangle 10" Padding="15">
                <Grid ColumnDefinitions="*, Auto">
                    <Label Text="TOTAL DA REFEIÇÃO" TextColor="White" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Column="1" Text="{Binding TotalCalories, StringFormat='{0} kcal'}" TextColor="White" FontSize="20" FontAttributes="Bold"/>
                </Grid>
            </Border>

            <Button Text="SALVAR TUDO" 
                    Command="{Binding SaveCommand}" 
                    BackgroundColor="#FF4081" 
                    TextColor="White" 
                    FontAttributes="Bold" 
                    CornerRadius="25" 
                    HeightRequest="50"/>
        </VerticalStackLayout>
    </Grid>
</ContentPage>